apiVersion: v1
kind: Service
metadata:
  name: {{ include "checklist-service.fullname" . }}
  labels:
    {{- include "checklist-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "checklist-service.selectorLabels" . | nindent 4 }}
